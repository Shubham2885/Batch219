package com.bridgelabz.home;

public class Locker {

	protected String propertiesFiles = "Plot property details";
	
}

//private -> variable, method, inner class, constructor, package => can't access outside the CLASS
//public -> variable, method, inner class, constructor, package =>  access through out the project
//default -> variable, method, inner class, constructor, package =>  access only within package
//protected -> variable, method, inner class, constructor, package =>  access only within package, 
//by extending the class/interface outside the package...